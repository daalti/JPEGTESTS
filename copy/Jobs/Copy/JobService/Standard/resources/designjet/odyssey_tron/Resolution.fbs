namespace dune.imaging.types;

/**< Resolution of the data that is output from the scan subsystem
 * (may be different than actual native scan resolution, scan subsystem will chose that)
 * @note   NEEDTO Come up with use case to show how offsets, extents, scaling and resolution fit together
 */
enum Resolution:byte
{
  E75DPI = 0,
  E100DPI,
  E150DPI,
  E200DPI,
  E240DPI,
  E300DPI,
  E400DPI,
  E500DPI,
  E600DPI,
  E1200DPI
}