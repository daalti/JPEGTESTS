include "ImageMetaInfoData.fbs";
include "JobMetaInfo.fbs";
include "ImageTypes.fbs";

namespace dune.job;

table ImageMetaInfo {
  tag:dune.imaging.types.InstanceType;
  imageMetaInfoData:dune.imaging.types.ImageMetaInfoData;
}

// We may have global pipe meta info data, not only at image level
table PipeMetaInfoFb {
  imagesMetaInfo:[ImageMetaInfo];
  jobMetaInfo:JobMetaInfoData;
}