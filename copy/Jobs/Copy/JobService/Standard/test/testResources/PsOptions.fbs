namespace dune.imaging.types;

enum PsEncoding : byte
{
    AUTO = 0,
    ASCII,
    BINARY
}

enum PsPreserveLineWidth : byte
{
    DEFAULT =0,
    ON,
    OFF
}
table PsOptionsFb {
    psEncoding:PsEncoding=AUTO;
    psPreserveLineWidth:PsPreserveLineWidth=DEFAULT;
}

root_type PsOptionsFb;